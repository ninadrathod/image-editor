services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      # Hot-reload source during local development
      - ./backend/app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
